package com.lld.filesystem.model;

import lombok.*;
@Getter @Setter @AllArgsConstructor @NoArgsConstructor
public class User {
    private String username;
    // Optionally, other fields like userId, group, etc.
}